import { paths } from 'core'
import { Form } from 'modules'

const Auth = () => {
    console.log(paths.register)
    return (
        <div className='auth'>

            <Form />

        </div>
    )

}


export default Auth
